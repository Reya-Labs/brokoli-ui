import styled from '@emotion/styled';

import { colors } from '../../foundation/Colors';

export const HorizontalLine = styled('div')`
  box-sizing: border-box;
  width: 100%;
  border: 0.5px solid ${colors.lavenderWeb5};
`;
